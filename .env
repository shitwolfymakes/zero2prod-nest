# Local development configuration. Committed for demo convenience only — a real
# service would keep this out of version control and inject secrets at deploy
# time.
NODE_ENV=development
PORT=8000
DATABASE_URL=postgres://postgres:password@localhost:5432/newsletter_nest
LOG_LEVEL=trace
